[DEFAULT]
# Database URL - SQLite database file
database = sqlite:///search.db

# Path to migration files
sources = migrations

# Verbosity level
verbosity = 1

# Whether to prompt before applying migrations
batch_mode = off